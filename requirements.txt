# Python 3.10+ recommended. Install PyTorch with CUDA separately if needed — see CLUSTER_RUNBOOK.md.

torch>=2.4.0
# assistant_only_loss for chat JSONL requires trl>=0.19
trl>=0.19.0,<0.30.0
transformers>=4.46.0,<5.0.0
datasets>=3.0.0
accelerate>=1.0.0
deepspeed>=0.15.4
huggingface_hub>=0.26.0
tensorboard>=2.14.0
hf_transfer>=0.1.9
pyyaml>=6.0
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=4.25.0
einops>=0.8.0

# Optional: faster Hub downloads (also set HF_HUB_ENABLE_HF_TRANSFER=1)
# Optional: wandb — only if you change report_to to wandb in sft_config.yaml
# Optional: flash-attn — pip install flash-attn --no-build-isolation (faster attention)
